# MacBook8,1: prefer suspend-to-idle so the GSPI/SPI keyboard controller
# stays powered across lid-close suspend. Deep S3 loses IRQ 21 and kills
# the built-in keyboard + trackpad until a hard reboot.
#
# Drop-in only — delete this file to revert.
[Sleep]
MemorySleepMode=s2idle
